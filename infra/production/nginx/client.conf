server {
  listen 8080;
  server_name _;
  server_tokens off;

  root /usr/share/nginx/html;
  charset utf-8;

  location = /healthz {
    access_log off;
    default_type text/plain;
    return 200 "ok\n";
  }

  location = /openBMB {
    return 308 /openBMB/;
  }

  location = /openBMB/index.html {
    add_header Cache-Control "no-store" always;
    try_files $uri =404;
  }

  location ^~ /openBMB/assets/ {
    add_header Cache-Control "public, max-age=31536000, immutable" always;
    try_files $uri =404;
  }

  location ^~ /openBMB/ {
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    try_files $uri $uri/ /openBMB/index.html;
  }

  location / {
    return 404;
  }
}
